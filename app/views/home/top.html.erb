<div class="container">
<div class="top-main">
</div>

<% if user_signed_in? %>
  <%= current_user.name %>
  <%= current_user.email %>
  <%= image_tag(current_user.image.url) if current_user.image.url %>
  <%= link_to "記録する", new_book_path %>
  <%= link_to "記録一覧", books_path %>
  <%= link_to "ユーザー編集", edit_user_registration_path %>
  <%= link_to "ログアウト",destroy_user_session_path, method: :delete %>
<% end %>

<div class="functions">
<h2>Dokkanの主な機能</h2>
<p>本の感想を記録すると、</p>
<p>「本ごと」「見出しごと」に記録を確認することができます</p>

<ul class="fruid-box">
  <li>
    <h4>見出しをつけて内容を記録します</h4>
    <p>見出しをつけることで、</p>
    <p>後で見出しごとに確認できます。</p>
    <div class="functions-image">
    </div>
  </li>

  <li>
    <h4>メニューが作られます</h4>
    <p>記録すると</p>
    <p>自動でメニューが作られます。</p>
    <div class="functions-image">
    </div>
  </li>

  <li>
    <h4>まとめられた記録を確認できます</h4>
    <p>それぞれのメニューをクリックすると</p>
    <p>まとめられた記録を確認できます。</p>
    <div class="functions-image">
    </div>
  </li>
</ul>
</div>

<footer>
  <p>Copyright ©️ 2018 watepu All RIGHTS RESERVED.</p>
</footer>
</div>
