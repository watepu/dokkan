<h2>記録画面</h2>

<%= form_with(model: @book, local: true) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :headline %>
  <%= f.text_field :headline %>

  <%= f.label :content %>
  <%= f.text_area :content %>

  <%= f.submit "記録する" %>
<% end %>
